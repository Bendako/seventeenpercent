// /** @type {import('next').NextConfig} */
// const nextConfig = {};

// export default nextConfig;

export const images = {
    remotePatterns: [
        {
            protocol: 'https',
            hostname: 'daisyui.com',
            port: '',
            //   pathname: '/account123/**',
        },
    ],
};